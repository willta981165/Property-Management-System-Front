{
  "name": "app-community-building",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/app-community-building/src",
  "prefix": "stitch",
  "targets": {
    "build": {
      "executor": "@angular-devkit/build-angular:browser",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/app-community-building",
        "index": "apps/app-community-building/src/index.html",
        "main": "apps/app-community-building/src/main.ts",
        "polyfills": ["zone.js"],
        "tsConfig": "apps/app-community-building/tsconfig.app.json",
        "assets": [
          {
            "glob": "**/*",
            "input": "apps/app-community-building/src/assets",
            "output": "assets"
          }
        ],
        "styles": ["apps/app-community-building/src/styles.scss"],
        "stylePreprocessorOptions": {
          "includePaths": ["libs/ui-kits/src/styles"]
        },
        "scripts": []
      },
      "configurations": {
        "production": {
          "budgets": [
            {
              "type": "initial",
              "maximumWarning": "2mb",
              "maximumError": "5mb"
            }
          ],
          "outputHashing": "all"
        },
        "development": {
          "buildOptimizer": false,
          "optimization": false,
          "vendorChunk": true,
          "extractLicenses": false,
          "sourceMap": true,
          "namedChunks": true
        }
      },
      "defaultConfiguration": "production"
    },
    "serve": {
      "executor": "@angular-devkit/build-angular:dev-server",
      "configurations": {
        "production": {
          "buildTarget": "app-community-building:build:production"
        },
        "development": {
          "buildTarget": "app-community-building:build:development"
        }
      },
      "defaultConfiguration": "development"
    },
    "cap:sync": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx build app-community-building", "npx cap sync"],
        "parallel": false
      }
    },
    "cap:add:android": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx cap add android"
      }
    },
    "cap:add:ios": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx cap add ios"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx build app-community-building", "npx cap sync"],
        "parallel": false
      }
    },
    "deploy:android": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx build app-community-building", "npx cap sync android", "npx cap open android"],
        "parallel": false
      }
    },
    "deploy:ios": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx build app-community-building", "npx cap sync ios", "npx cap open ios"],
        "parallel": false
      }
    }
  }
}
